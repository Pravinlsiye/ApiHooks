{
  "name": "Conditional Workflow Example",
  "description": "Demonstrates conditional execution and flow control",
  "variables": {
    "checkUserId": 999,
    "fallbackUserId": 1
  },
  "steps": [
    {
      "id": "check-user-exists",
      "name": "Check if User Exists",
      "description": "Try to fetch a user that might not exist",
      "operationId": "getUser",
      "parameters": {
        "userId": "{{checkUserId}}"
      },
      "extractVariables": {
        "userFound": "$.id",
        "userName": "$.name"
      },
      "onSuccess": {
        "goto": "fetch-user-posts"
      },
      "onFailure": {
        "setVariables": {
          "userFound": false
        },
        "goto": "fetch-fallback-user"
      }
    },
    {
      "id": "fetch-fallback-user",
      "name": "Fetch Fallback User",
      "description": "Get a known existing user as fallback",
      "operationId": "getUser",
      "parameters": {
        "userId": "{{fallbackUserId}}"
      },
      "extractVariables": {
        "userName": "$.name",
        "userEmail": "$.email"
      },
      "onFailure": {
        "stop": true
      }
    },
    {
      "id": "fetch-user-posts",
      "name": "Fetch User Posts",
      "description": "Get posts for the found user",
      "operationId": "getPosts",
      "parameters": {
        "userId": "{{userFound == false ? fallbackUserId : checkUserId}}"
      },
      "extractVariables": {
        "posts": "$",
        "postCount": "$.length"
      }
    },
    {
      "id": "create-summary-post",
      "name": "Create Summary Post",
      "description": "Create a post summarizing the workflow execution",
      "operationId": "createPost",
      "condition": "{{postCount}} != null",
      "body": {
        "userId": 1,
        "title": "Workflow Execution Summary",
        "body": "User '{{userName}}' has {{postCount}} posts. This workflow used {{userFound == false ? 'fallback' : 'primary'}} user."
      }
    }
  ]
}
